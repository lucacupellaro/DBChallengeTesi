apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: volatility
    user: flink
    secureJsonData:
      password: flink
    jsonData:
      sslmode: disable
      postgresVersion: 1600
    isDefault: true
    editable: false
